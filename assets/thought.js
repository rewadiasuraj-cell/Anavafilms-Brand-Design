/* Scroll to Direct: an illustrative treatment of a sample still. */
let thoughtTimeline,thoughtMedia;
let thoughtDraft='';
const directorStages=[['The raw frame.','A starting point. Light, character and possibility.'],['Find the feeling.','Colour changes the mood. The story starts to emerge.'],['Give it a voice.','A few words. Placed with purpose.'],['Make it a film.','Colour, type and composition. One connected idea.']];
function thoughtExperiment(){return `<section class="direct-lab" aria-labelledby="direct-heading"><div class="direct-sticky"><div class="direct-heading"><div><span class="eyebrow">SCROLL TO DIRECT / ANAVA</span><h2 id="direct-heading">You move. <em>The frame follows.</em></h2></div><button class="direct-skip" type="button" data-direct="3">Skip to the final frame ↓</button></div><div class="direct-screen"><img class="direct-raw" src="assets/fashion.jpg" alt="Sample fashion portrait used to demonstrate a creative treatment" width="1600" height="1100" loading="lazy"><img class="direct-colour" src="assets/fashion.jpg" alt="" aria-hidden="true" width="1600" height="1100" loading="lazy"><div class="direct-shade" aria-hidden="true"></div><div class="direct-bar direct-bar-top" aria-hidden="true"></div><div class="direct-bar direct-bar-bottom" aria-hidden="true"></div><div class="direct-guides" aria-hidden="true"><span></span><span></span></div><span class="direct-frame-note">ANAVA / FRAME 001</span><div class="direct-type" aria-hidden="true"><span>Ordinary?</span><em>Not today.</em></div><div class="direct-final-credit" aria-hidden="true">A THOUGHT. AN IDEA. A FRAME.</div><span class="direct-treatment">Illustrative treatment · Sample still</span></div><div class="direct-console"><div class="direct-stage-copy"><h3 id="direct-stage-title">The raw frame.</h3><p id="direct-stage-caption">A starting point. Light, character and possibility.</p></div><div class="direct-chapters" role="group" aria-label="Explore the four creative stages">${['Raw frame','Colour','Typography','Final frame'].map((s,i)=>`<button type="button" data-direct="${i}" aria-pressed="${i===0}" class="${i===0?'active':''}"><small>0${i+1}</small><span>${s}</span></button>`).join('')}</div></div><div class="direct-progress" aria-hidden="true"><span></span></div><div class="direct-bottom"><span class="direct-instruction">Scroll through the process, or choose a stage.</span><a href="#contact" id="direct-enquire">Let’s make your next frame ↗</a></div></div></section>`}
function disposeThought(){if(thoughtMedia){thoughtMedia.revert();thoughtMedia=null}if(thoughtTimeline){thoughtTimeline.kill();thoughtTimeline=null}}
function initThought(){const root=document.querySelector('.direct-lab');if(!root)return;const $=s=>root.querySelector(s);let current=-1,scrollActive=false;
function stageText(index){if(index===current)return;current=index;$('#direct-stage-title').textContent=directorStages[index][0];$('#direct-stage-caption').textContent=directorStages[index][1];root.querySelectorAll('.direct-chapters button').forEach((b,i)=>{b.classList.toggle('active',i===index);b.setAttribute('aria-pressed',i===index)})}
function manual(index){stageText(index);$('.direct-colour').style.opacity=index>=1?'1':'0';$('.direct-shade').style.opacity=index>=2?'1':'0';$('.direct-type').style.opacity=index>=2?'1':'0';$('.direct-guides').style.opacity=index===0?'1':'0';$('.direct-final-credit').style.opacity=index===3?'1':'0';root.querySelectorAll('.direct-bar').forEach(b=>b.style.transform=index===3?'scaleY(1)':'scaleY(0)');$('.direct-progress span').style.transform=`scaleX(${index/3})`}
if(window.gsap&&window.ScrollTrigger){gsap.registerPlugin(ScrollTrigger);thoughtMedia=gsap.matchMedia();thoughtMedia.add({motion:'(prefers-reduced-motion: no-preference)',tall:'(min-height: 800px)',desktop:'(min-width: 761px)'},ctx=>{if(!ctx.conditions.motion||!ctx.conditions.tall||!ctx.conditions.desktop){root.classList.remove('scroll-enabled');scrollActive=false;$('.direct-instruction').textContent='Choose a stage to explore the treatment.';manual(0);return}
root.classList.add('scroll-enabled');scrollActive=true;$('.direct-instruction').textContent='Scroll through the process, or choose a stage.';
gsap.set([$('.direct-colour'),$('.direct-shade'),$('.direct-type'),$('.direct-final-credit')],{opacity:0});gsap.set('.direct-bar',{scaleY:0});gsap.set('.direct-guides',{opacity:1});
thoughtTimeline=gsap.timeline({scrollTrigger:{trigger:root,start:'top 110px',end:'bottom bottom-=25px',scrub:.45,invalidateOnRefresh:true},onUpdate(){const p=this.progress();stageText(p<.23?0:p<.48?1:p<.77?2:3)}});
thoughtTimeline.to('.direct-guides',{opacity:0,duration:.15},.12).to('.direct-colour',{opacity:1,duration:.23,ease:'none'},.08).to('.direct-shade',{opacity:1,duration:.2},.37).fromTo('.direct-type',{opacity:0,y:30},{opacity:1,y:0,duration:.23,ease:'power2.out'},.38).to('.direct-bar',{scaleY:1,duration:.2},.69).to('.direct-final-credit',{opacity:1,duration:.17},.78).to('.direct-progress span',{scaleX:1,duration:1,ease:'none'},0);
const image=$('.direct-raw');const refresh=()=>ScrollTrigger.refresh();if(!image.complete)image.addEventListener('load',refresh,{once:true});return()=>{image.removeEventListener('load',refresh);root.classList.remove('scroll-enabled');scrollActive=false};});
}else {manual(0);$('.direct-instruction').textContent='Choose a stage to explore the treatment.';}
root.querySelectorAll('[data-direct]').forEach(button=>button.addEventListener('click',()=>{const i=Number(button.dataset.direct);if(scrollActive&&thoughtTimeline?.scrollTrigger){const st=thoughtTimeline.scrollTrigger;const points=[0,.34,.64,1];window.scrollTo({top:st.start+(st.end-st.start)*points[i],behavior:'smooth'})}else manual(i)}));
$('#direct-enquire').addEventListener('click',()=>{thoughtDraft='I’d like to explore a film with a distinctive colour treatment, typography and a memorable final composition.'});
}
function applyThoughtDraft(){if(!thoughtDraft)return;const field=document.querySelector('#enquiry textarea');if(field){field.value=thoughtDraft;thoughtDraft=''}}
